package network.oxalis.as4.inbound.multi;

public class As4MultiCertConstants {

	/**
	 * 
	 * Key to store an EndpointConfigData object, used to configure endpoint, into endpoint properties.
	 * 
	 * Filled on endpoint publishing and passed to Message properties on by endpoint selector.
	 */
	public static final String MULTI_CERT_ENDPOINT_CONFIG_DATA = "oxalis.as4.inbound.multi.config";
}
